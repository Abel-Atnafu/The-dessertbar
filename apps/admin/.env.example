# PostgreSQL connection string from Neon / Vercel Postgres
# Same database as the web app
DATABASE_URL="postgresql://..."

# Must match the JWT_SECRET in the web app exactly
JWT_SECRET="your-secret-here"
